namespace Vlingo.Xoom.Streams
{
  public class Stream
  {
    public static readonly long DEFAULT_FLOW_RATE = 100;
    public static readonly int DEFAULT_PROBE_INTERVAL = PublisherConfiguration.DEFAULT_PROBE_INTERVAL;
    public static readonly int FAST_PROBE_INTERVAL = PublisherConfiguration.FAST_PROBE_INTERVAL;
    public static readonly int FASTEST_PROBE_INTERVAL = PublisherConfiguration.FASTEST_PROBE_INTERVAL;
  }
}